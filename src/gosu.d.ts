declare global {
    interface Gosu {
        settings: {
            showInterface: boolean,
            folders: {
                game: string,
                skin: string,
                songs: string,
            },
        },
    }
}

export { };
